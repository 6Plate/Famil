package Main;

import Main.Main;

import java.io.Console;
import java.time.LocalDate;
import Model.Human.Gender;
import Model.Human.Human;
import Model.Service.Service;
import Model.Tree.FamilyTree.FamilyTree;
import View.ConsoleUI;

public class Main {

public static void main(String[] args) {
    ConsoleUI console = new ConsoleUI();
    console.start();
    // Service service = new Service();
    // service.addItem("Платон", Gender.Male, LocalDate.of(2004, 4, 12));
    // service.addItem("Кирилл", Gender.Male, LocalDate.of(1976, 11, 6));
    // service.addItem("Юлия", Gender.Female, LocalDate.of(1978, 8, 30));
    // System.out.println(service.getHumanInfo());
    // service.AddParentById(0, 1);
    // System.out.println(service.getHumanInfo());
    // service.AddChildById(1, 0);
    // System.out.println(service.getHumanInfo());
    // System.out.println(service.sortByName());

    


    
}
}